import React from 'react';
import ReactDOM from 'react-dom/client';
import ExpediaFlights from './expedia-flights';

const root = document.getElementById('root');
if (root) {
  ReactDOM.createRoot(root).render(
    <React.StrictMode>
      <ExpediaFlights />
    </React.StrictMode>
  );
}









