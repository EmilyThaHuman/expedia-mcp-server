import React from 'react';
import ReactDOM from 'react-dom/client';
import ExpediaHotels from './expedia-hotels';

const root = document.getElementById('root');
if (root) {
  ReactDOM.createRoot(root).render(
    <React.StrictMode>
      <ExpediaHotels />
    </React.StrictMode>
  );
}









